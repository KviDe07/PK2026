# ============================================================================
#  Параметры контактов (идентичность абитуриента).
#  Поля создаются командой `admissions setup-contact-fields --apply`;
#  коды резолвятся по XML_ID в рантайме.
#  Значения «тип поступающего» (enum) задаются в bitrix_fields.py.
# ============================================================================

# Возможные значения поля «Тип поступающего».
type_values:
  - Бакалавриат
  - Магистратура
  - Аспирантура
